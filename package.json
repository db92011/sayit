{
  "name": "sayit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cap:add:ios": "npx cap add ios",
    "cap:copy": "npx cap copy",
    "cap:open:ios": "npx cap open ios",
    "cap:sync": "npx cap sync ios",
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1 -d site",
    "test": "node --test test/*.test.js",
    "test:smoke": "node test/preview-smoke.mjs"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/core": "^8.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0",
    "@capacitor/ios": "^8.2.0"
  }
}
